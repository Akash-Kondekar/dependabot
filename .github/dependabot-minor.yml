version: 2
updates:
    - package-ecosystem: npm
      directory: "/"
      target-branch: main
      schedule:
          interval: monthly
      # Minor updates only
      allow:
          - update-type: version-update:semver-minor
